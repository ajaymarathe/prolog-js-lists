// (*) find the last elements of the list.

function FindLastElement(list){
    let lastElement = list[list.length - 1];
    console.log('last elements:',lastElement);
    return lastElement;
}

FindLastElement(['h','e','l','l','o']);